export interface Product {
  id: string;
  category_id: string;
  store_id: string;
  name: string;
  description: string;
  price: number;
  picture_url: string;
  list_price: string;
  size: string;
  color: string;
  quantity: string;
  enable: boolean;
}
